//проверка является ли треугольником фигура с тремя заданными сторонами

function isTriangle(a,b,c)
{
  if ( a < b + c && a > b - c && b < a + c && b > a - c && c < a + b && c > a - b) {
 return true;
  }
  else return false;
}
